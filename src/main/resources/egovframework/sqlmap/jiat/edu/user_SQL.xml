<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="User">

    <!-- 아이디로 회원 조회 -->
    <select id="findByUsername" resultType="egovframework.com.cms.edu.model.UserVO">
        SELECT
            id,
            username,
            password,
            name,
            created_at AS createdAt,
            is_admin AS isAdmin
        FROM edu_user
        WHERE username = #{username}
    </select>

    <!-- 회원 가입 -->
    <insert id="insertUser" parameterType="egovframework.com.cms.edu.model.UserVO">
        INSERT INTO edu_user (username, password, name)
        VALUES (#{username}, #{password}, #{name})
    </insert>

    <!-- 아이디 중복 체크 -->
    <select id="countByUsername" resultType="int">
        SELECT COUNT(*)
        FROM edu_user
        WHERE username = #{username}
    </select>

    <!-- 회원 탈퇴 -->
    <delete id="deleteUser" parameterType="long">
        DELETE FROM edu_user
        WHERE id = #{id}
    </delete>

</mapper>

